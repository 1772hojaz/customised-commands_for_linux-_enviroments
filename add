#!/bin/zsh
autoload -U colors && colors
git add .
echo "Write the commit message: "
read -r P
git commit -m "$P"

if [ $? -ne 0 ]; then
    echo -e "${fg[red]}Commit failed${reset_color}"
    exit 1
else:
	git push
	echo -e "${fg[green]}Pushed to remote${reset_color}"
fi
